@{
    Python = @{
        Prompts = @(
            @{
                Name        = 'CodeRefactor'
                Description = "Suggest a way to refactor the following '{0}' code to be more readable and modular."
            }
        )
    }
}